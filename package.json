{
  "name": "collie-dsl",
  "version": "0.1.0",
  "description": "Collie DSL Compiler — compiles Collie (.cl) to TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "echo 'no linter configured'"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
